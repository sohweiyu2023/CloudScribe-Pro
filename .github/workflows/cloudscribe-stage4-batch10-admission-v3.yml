name: CloudScribe Stage 4 foundation batch 10 admission v5
# canonical self-consistent LF candidate
# registration trigger v3

on:
  workflow_dispatch:
  push:
    branches: [cloudscribe-stage4-batch10-dev]
    paths: [.github/workflows/cloudscribe-stage4-batch10-admission-v3.yml]

concurrency:
  group: cloudscribe-stage4-batch10-admission-v5
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  admit:
    runs-on: windows-2025
    steps:
      - uses: actions/checkout@v6
        with:
          ref: cloudscribe-stage4-batch10-dev
          path: carrier
          fetch-depth: 1
      - uses: actions/checkout@v6
        with:
          ref: cloudscribe-0.5.0-stage4
          path: source
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: '3.13'
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.400'
          cache: true
          cache-dependency-path: source/**/packages.lock.json
      - name: Run exact Batch 10 v5 admission
        shell: pwsh
        run: |
          $ErrorActionPreference='Stop'
          $root='carrier/.cloudscribe-ci/stage4-batch10'
          $part1=Join-Path $root 'admit-v3-01.part'
          $part2=Join-Path $root 'admit-v3-02.part'
          $blob1=(git -C carrier hash-object '.cloudscribe-ci/stage4-batch10/admit-v3-01.part').Trim()
          $blob2=(git -C carrier hash-object '.cloudscribe-ci/stage4-batch10/admit-v3-02.part').Trim()
          if($blob1 -ne '57cd8913cff4760a13bf3ea8b3b6fafc63a2bec8'){throw "Admission runner part 1 blob mismatch: $blob1"}
          if($blob2 -ne '6b77dfb7b7d85f28e883d1c2f8bd4458f99a714c'){throw "Admission runner part 2 blob mismatch: $blob2"}
          $script=Join-Path $env:RUNNER_TEMP 'admit-batch10-v5.ps1'
          $text=[IO.File]::ReadAllText($part1)+[IO.File]::ReadAllText($part2)
          [IO.File]::WriteAllText($script,$text,[Text.UTF8Encoding]::new($false))
          & $script -SourceRoot source -CarrierRoot carrier
          if($LASTEXITCODE -ne 0){throw 'Batch 10 v5 admission failed.'}
      - name: Upload Batch 10 v5 evidence
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: cloudscribe-stage4-foundation-batch10-admission-v5
          retention-days: 14
          if-no-files-found: warn
          path: |
            ${{ runner.temp }}/stage4-batch10-v5-tests/**
            ${{ runner.temp }}/stage4-batch10-v5-package/**
            ${{ runner.temp }}/batch10-v5-candidate.patch
