CloudScribe Pro — Stage8 Production Recovery Handover
Date: 2026-09-01
Branch: repair/final-stage8-production-parallel
Current substantive head before this handover: cdad40dc8fe188f7208a8c78cee4c0277e3d7cb9

This run removed the restart-recovery plan-provenance gap without ambient reconstruction.

Landed production changes:
- fdba232e33085ac291dd52afb823740287852ccd — RestoreTransactionJournal now persists the exact RestoreExecutionPlan inside the authenticated journal payload. RequirePersistedPlan fails closed when the plan is absent and independently verifies that the persisted plan still matches PlanSha256.
- e858019dcaccf2ab1b437a952666dc3ad1e6e4c6 — RestoreRecoveryExecutionService now exposes RecoverPersistedAsync, which loads the authenticated journal and recovers only from its hash-bound persisted plan. It also rejects a relative backup root before normalization.
- 7f8bf8ad496fccaf4a04a86f23a842157b831873 — Stage8RestoreRecoveryShellBinder now exposes ConfigurePersistedRecovery, removing any dependency on ambient/current-plan reconstruction for restart recovery while preserving explicit fully-qualified path checks.
- cdad40dc8fe188f7208a8c78cee4c0277e3d7cb9 — focused domain regression tests lock persisted-plan presence and reject missing or hash-mismatched restart plans.

Safety rationale:
The recovery journal HMAC authenticates the serialized transaction payload, including the persisted execution plan. PlanSha256 independently binds the transaction to that exact plan. Restart recovery refuses legacy journals without an authenticated persisted plan and rejects plan swaps whose digest no longer matches. No working-directory state, current UI state, or fabricated plan is used to reconstruct restore authorization.

Gate state:
- Authenticated persisted-plan restart recovery: PASS by source inspection.
- Focused compiled tests: NOT RUN.
- Exact source-head GitHub Actions for cdad40dc8fe188f7208a8c78cee4c0277e3d7cb9: NOT RUN; GitHub returned zero workflow runs.
- CompositionRoot Stage8 production registration: NOT COMPLETE.
- Final Windows executable acceptance / strict-full / exact-SHA no-change certification: NOT RUN.

Still open:
- CompositionRoot still does not invoke Stage8RestoreRecoveryShellBinder.ConfigurePersistedRecovery.
- Production composition still needs a genuine persisted/configured CredentialReference for restore-journal authentication. Do not fabricate or hardcode this reference.
- Explicit journal and staging paths must be derived from trusted production AppPaths; the backup root already has a trusted AppPaths source.
- Stage6 and Stage7 production composition remain independent open lanes.

Next exact Stage8 gate:
Bind trusted AppPaths-based journal/staging/backup paths and the real persisted/configured restore-journal authentication CredentialReference into CompositionRoot -> Stage8RestoreRecoveryShellBinder.ConfigurePersistedRecovery, then add fail-closed composition tests and run/inspect the exact compiled gate when available.
